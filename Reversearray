class Solution
{
	public:
		vector<int> Reversearray(vector<int> & nums)
		{
			vector<int>vec;
			for(int i=nums.size()-1;i>=0;i--)
			{
				vec.push_back(nums[i]);
			}

			
			return vec;
			
		}
};
